# =====================================================
# Liquibase configuration - Somnguard DB
# Credentials come from environment variables:
#   LIQUIBASE_URL, LIQUIBASE_USERNAME, LIQUIBASE_PASSWORD
# See scripts/db.ps1 (loads them from .env) or set them manually.
# =====================================================

# Main changelog entry point
changeLogFile=changelog/changelog-master.yaml

# PostgreSQL JDBC driver
classpath=C:/Users/sampf/.liquibase/lib/postgresql-42.7.7.jar
driver=org.postgresql.Driver

# Schema
defaultSchemaName=public
liquibaseSchemaName=public
